# Cloudflare Pages configuration
# Deploy: npx wrangler pages deploy . --project-name accountability-os

name = "accountability-os"
compatibility_date = "2024-01-01"

# No build step needed — static HTML + ES modules
# Just deploy the root directory as-is

[site]
bucket = "."
